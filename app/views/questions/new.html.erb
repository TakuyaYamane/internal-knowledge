<h1>社内質問</h1>

<%= form_with url: questions_path, method: :get do |form| %>
  <div>
    <%= form.label :question, "質問" %>
    <%= form.text_area :question %>
  </div>

  <div>
    <%= form.submit "質問する" %>
  </div>
<% end %>

<p><%= link_to "ナレッジ一覧へ", knowledges_path %></p>
